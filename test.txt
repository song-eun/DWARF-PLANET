


	#push 전엔 pull이 있다

	#push: git저장소로 자료를 올리는것
	#pull: 반대로 땡겨오는것

	#pull
	#git pull orgin master: pull을 하는 커맨드, 지정해놓은 저장소에서 끌어온다.

	#push
	#git status: 현재 나의 로컬폴더와 git과의 싱크 상태를 체크
	#git add .(점): 로컬폴더와 git저장소 사이에 변경된 파일이 있을때 그 모든 파일들을 트랙킹
	#git commit -m "message": 커밋을 다는 명령어 (변경사항을 git에 업로드 했을때 내가 나에게 알려주는 커밋이나 다른 팀원에게 알려주는 커밋을 달 수 있는 명령어)
	#git push orgin +master: 실제로 git에 push 
	
	# 추가된 메세지입니다.
	# 추가된 메세지입니다.